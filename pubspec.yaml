name: dartstream_smoke
description: End-to-end smoke CLI for the DartStream dev API.
publish_to: none

# Min Dart 3.12.0; tracks latest stable (^ accepts newer). See README Prerequisites.
environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.2.2
